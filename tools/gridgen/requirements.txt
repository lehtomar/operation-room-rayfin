# gridgen requirements (install into the interpreter that `python` runs):
#   python -m pip install -r tools/gridgen/requirements.txt
geopandas>=1.0
shapely>=2.0
pyproj>=3.6
networkx>=3.0
scikit-learn>=1.3
scipy>=1.10
pyarrow>=15.0
